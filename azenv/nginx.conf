# azenv · 请求环境回显（纯 nginx SSI）
# 放入服务 0x5c0f.cc 的 server {} 即可；依赖 nginx 自带 SSI 模块。

location = /azenv {
    return 301 /azenv/;
}
location = /azenv/ {
    ssi on;
    try_files /azenv/index.shtml =404;
}
location ^~ /azenv/ {
    ssi on;
    try_files $uri =404;
}
